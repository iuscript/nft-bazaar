type User @entity {
  id: ID!
  tokens: [Nft!]! @derivedFrom(field: "holder")
}

type Nft @entity {
  id: ID!
  tokenURI: String
  createdAtTimestamp: BigInt!
  createdAtBlockNumber: BigInt!
  holder: User!
}
